# Portfolio v3 Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the 1800-line v2 `index.html` with a calm, single-page editorial site (layout C, classic serif, forest-green accent) backed by a Playwright smoke test and a CI gate.

**Architecture:** One hand-written `index.html` with embedded CSS and one small script. Three reusable patterns (`.section`, `.row`, `.card`) carry every entry; content is inline HTML. Tests run Playwright against the file over `file://`, locally via `npm test` and in GitHub Actions on every push.

**Tech Stack:** HTML, CSS, vanilla JS, Google Fonts (Newsreader, Inter), `@playwright/test`, GitHub Actions, macOS `sips` for one-time image resizing.

**Spec:** `DESIGN.md` at the repo root.

## Global Constraints

- Single `index.html`; no build step; no JS or icon libraries; Google Fonts is the only external dependency.
- Motion is hover only. No scroll or load animations. No IntersectionObserver.
- Closed token list (copy exactly): `--paper:#fbf9f4 --ink:#1a1c19 --muted:#6a6e67 --line:#e6e3da --accent:#2f6b45 --tint-1:#e6ede4 --tint-2:#f0e8d9 --tint-3:#e3e8ee --tint-4:#eee3e8`. No new colors.
- Two fonts only: Newsreader (headings, body) and Inter (labels, dates, nav, buttons).
- Container width 880px, radius 10px, spacing base 8px. One breakpoint at 640px.
- Works at 390px viewport with no horizontal scroll.
- `api/`, `vercel.json`, `package.json` engines field, `resume.pdf`, `CNAME` are not removed. `package.json` may gain devDependencies and a `test` script only.
- Copy comes from the DESIGN.md appendix verbatim. It is marked DRAFT there; the user edits it after the build, not the implementer.
- Commits: no `Co-Authored-By` lines (repo rule in `~/projects/CLAUDE.md`).
- All work happens on branch `feat/v3-redesign`, never on `main`.

## File map

| File | Responsibility | Action |
|---|---|---|
| `index.html` | The whole site: head, CSS, six sections, one script | Rewrite |
| `tests/smoke.spec.js` | Playwright smoke test | Create |
| `playwright.config.js` | Test runner config (chromium, `tests/` dir) | Create |
| `package.json` | Add `@playwright/test` devDependency and `test` script | Modify |
| `package-lock.json` | Generated by npm install | Create |
| `.github/workflows/test.yml` | Run `npm test` on push and PR | Create |
| `.gitignore` | Add `node_modules/`, `test-results/`, `playwright-report/` | Modify |
| `images/logo_*.png` | Downscale six logos to 160px long edge | Modify |
| `CLAUDE.md`, `AGENTS.md` | Project notes for future sessions (local, untracked) | Rewrite |

---

### Task 1: Test harness and CI gate on a feature branch

**Files:**
- Modify: `package.json`
- Modify: `.gitignore`
- Create: `playwright.config.js`
- Create: `tests/smoke.spec.js`
- Create: `.github/workflows/test.yml`

**Interfaces:**
- Produces: `npm test` command; `tests/smoke.spec.js` exporting a `URL` constant pointing at `index.html` over `file://`, which Task 2 extends.

- [ ] **Step 1: Create the branch**

```bash
cd /Users/sambhav/projects/portfolio_v2
git checkout -b feat/v3-redesign main
```

- [ ] **Step 2: Install Playwright and register the test script**

```bash
npm install --save-dev @playwright/test@1.47.2
npx playwright install chromium
```

Then edit `package.json` so it reads exactly:

```json
{
  "name": "portfolio-v2",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2"
  }
}
```

(Keep whatever exact version npm wrote in `devDependencies`; the point is that `scripts.test` exists and `engines` is unchanged.)

- [ ] **Step 3: Ignore generated files**

Append to `.gitignore`:

```
node_modules/
test-results/
playwright-report/
```

- [ ] **Step 4: Write the runner config**

Create `playwright.config.js`:

```js
// Runs the smoke test against index.html opened directly from disk.
const { defineConfig } = require('@playwright/test');

module.exports = defineConfig({
  testDir: 'tests',
  reporter: 'list',
  use: { browserName: 'chromium' },
});
```

- [ ] **Step 5: Write the first smoke test**

Create `tests/smoke.spec.js`:

```js
const { test, expect } = require('@playwright/test');
const path = require('path');

// index.html is opened straight from disk; no server needed.
const URL = 'file://' + path.resolve(__dirname, '..', 'index.html');

test('page loads with the right title', async ({ page }) => {
  await page.goto(URL);
  await expect(page).toHaveTitle('Sambhav Shrestha');
});
```

- [ ] **Step 6: Run the test and confirm it passes against the current v2 file**

Run: `npm test`
Expected: `1 passed`

- [ ] **Step 7: Add the GitHub Actions workflow**

Create `.github/workflows/test.yml`:

```yaml
name: test
on:
  push:
  pull_request:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm test
```

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json .gitignore playwright.config.js tests/smoke.spec.js .github/workflows/test.yml
git commit -m "Add Playwright smoke test and CI workflow"
```

---

### Task 2: Write the v3 smoke tests (red)

**Files:**
- Modify: `tests/smoke.spec.js`

**Interfaces:**
- Consumes: `URL` from Task 1.
- Produces: the assertions every later task drives toward. Selectors used: `#top`, `#work`, `#projects`, `#education`, `#contact`, `#work .row`, `#projects .card`, `#education .card`, `button#show-all`, `.site-header`.

- [ ] **Step 1: Replace the test file with the full suite**

Overwrite `tests/smoke.spec.js`:

```js
const { test, expect } = require('@playwright/test');
const path = require('path');
const fs = require('fs');

// index.html is opened straight from disk; no server needed.
const ROOT = path.resolve(__dirname, '..');
const URL = 'file://' + path.join(ROOT, 'index.html');

test('page loads with the right title and no console errors', async ({ page }) => {
  const errors = [];
  page.on('pageerror', (e) => errors.push(e.message));
  page.on('console', (m) => { if (m.type() === 'error') errors.push(m.text()); });
  await page.goto(URL);
  await expect(page).toHaveTitle('Sambhav Shrestha');
  expect(errors).toEqual([]);
});

test('has the header and all five section ids', async ({ page }) => {
  await page.goto(URL);
  await expect(page.locator('.site-header')).toHaveCount(1);
  for (const id of ['top', 'work', 'projects', 'education', 'contact']) {
    await expect(page.locator('#' + id)).toHaveCount(1);
  }
});

test('work has four rows and education has two cards', async ({ page }) => {
  await page.goto(URL);
  await expect(page.locator('#work .row')).toHaveCount(4);
  await expect(page.locator('#education .card')).toHaveCount(2);
});

test('show all reveals eight project cards and hides the button', async ({ page }) => {
  await page.goto(URL);
  await expect(page.locator('#projects .card')).toHaveCount(8);
  await expect(page.locator('#projects .card:visible')).toHaveCount(4);
  const button = page.locator('button#show-all');
  await button.click();
  await expect(page.locator('#projects .card:visible')).toHaveCount(8);
  await expect(button).toBeHidden();
});

test('no horizontal scroll at phone width', async ({ page }) => {
  await page.setViewportSize({ width: 390, height: 844 });
  await page.goto(URL);
  const width = await page.evaluate(() => document.documentElement.scrollWidth);
  expect(width).toBeLessThanOrEqual(390);
});

test('every logo image resolves and is 160px or smaller', async ({ page }) => {
  await page.goto(URL);
  const sources = await page.locator('img').evaluateAll((imgs) => imgs.map((i) => i.getAttribute('src')));
  expect(sources.length).toBeGreaterThan(0);
  for (const src of sources) {
    expect(fs.existsSync(path.join(ROOT, src))).toBe(true);
  }
  const broken = await page.locator('img').evaluateAll((imgs) =>
    imgs.filter((i) => !i.complete || i.naturalWidth === 0).map((i) => i.getAttribute('src')));
  expect(broken).toEqual([]);
  const oversized = await page.locator('img').evaluateAll((imgs) =>
    imgs.filter((i) => Math.max(i.naturalWidth, i.naturalHeight) > 160).map((i) => i.getAttribute('src')));
  expect(oversized).toEqual([]);
});

test('outbound links are well formed and the resume exists', async ({ page }) => {
  await page.goto(URL);
  const hrefs = await page.locator('a[href]').evaluateAll((as) => as.map((a) => a.getAttribute('href')));
  const allowed = /^(#[a-z]+|mailto:[^@\s]+@[^@\s]+|https:\/\/(github\.com|linkedin\.com)\/\S+|\.\/resume\.pdf)$/;
  for (const href of hrefs) {
    expect(href, `unexpected href ${href}`).toMatch(allowed);
  }
  expect(fs.existsSync(path.join(ROOT, 'resume.pdf'))).toBe(true);
});
```

- [ ] **Step 2: Run the suite and confirm the new tests fail against v2**

Run: `npm test`
Expected: the title test may pass; at least `has the header and all five section ids`, `work has four rows`, `show all reveals`, and `every logo image` FAIL. That is the red state this plan builds out of.

- [ ] **Step 3: Commit the red tests**

```bash
git add tests/smoke.spec.js
git commit -m "Add v3 smoke tests (red until rewrite lands)"
```

---

### Task 3: Rewrite index.html skeleton: head, CSS, header, intro, contact

**Files:**
- Rewrite: `index.html` (the v2 content is discarded entirely; it remains in git history at commit `a5b2939`)

**Interfaces:**
- Produces: the complete stylesheet every later task relies on. Class names: `.wrap`, `.label`, `.meta`, `.link`, `.pill`, `.site-header`, `.intro`, `.section`, `.row` (+ `.logo .body .title .sub .role .desc .date`), `.grid`, `.card` (+ `.card-outline .title .desc .foot .logo`), `.show-all`, `.contact`. Section placeholders `#work`, `#projects`, `#education` are empty `<section>` elements that Tasks 4 to 6 fill.

- [ ] **Step 1: Replace index.html with the skeleton**

Overwrite `index.html` with exactly this:

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Sambhav Shrestha</title>
<meta name="description" content="Sambhav Shrestha, software engineer working on the infrastructure behind machine learning.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,400;0,6..72,500;1,6..72,400&family=Inter:wght@400;500&display=swap" rel="stylesheet">
<style>
/* ---- tokens (closed list, see DESIGN.md) ---- */
:root{
  --paper:#fbf9f4; --ink:#1a1c19; --muted:#6a6e67; --line:#e6e3da; --accent:#2f6b45;
  --tint-1:#e6ede4; --tint-2:#f0e8d9; --tint-3:#e3e8ee; --tint-4:#eee3e8;
  --serif:"Newsreader",Georgia,"Times New Roman",serif;
  --sans:"Inter",-apple-system,"Helvetica Neue",sans-serif;
  --measure:880px; --radius:10px; --s:8px;
}

/* ---- base ---- */
*,*::before,*::after{box-sizing:border-box}
html{scroll-behavior:smooth}
body{margin:0;background:var(--paper);color:var(--ink);font-family:var(--serif);font-size:17px;line-height:1.5;-webkit-font-smoothing:antialiased}
a{color:inherit;text-decoration:none}
img{max-width:100%;display:block}
h1,h2,h3,p{margin:0}
:focus-visible{outline:2px solid var(--accent);outline-offset:3px}
.wrap{max-width:var(--measure);margin:0 auto;padding:0 calc(var(--s)*3)}

/* ---- text roles ---- */
.label{font-family:var(--sans);font-size:11px;font-weight:500;letter-spacing:.14em;text-transform:uppercase;color:var(--muted)}
.meta{font-family:var(--sans);font-size:13px;color:var(--muted)}
.link{color:var(--accent);text-decoration:underline;text-decoration-color:transparent;text-underline-offset:3px;transition:text-decoration-color .15s}
.link:hover,.link:focus-visible{text-decoration-color:currentColor}
.pill{display:inline-block;font-family:var(--sans);font-size:13px;font-weight:500;line-height:1;padding:calc(var(--s)*1.25) calc(var(--s)*2);border:1px solid var(--ink);border-radius:999px;background:transparent;color:var(--ink);cursor:pointer;transition:background .15s,color .15s}
.pill:hover,.pill:focus-visible{background:var(--ink);color:var(--paper)}

/* ---- header ---- */
.site-header{display:flex;justify-content:space-between;align-items:baseline;flex-wrap:wrap;gap:var(--s) calc(var(--s)*3);padding:calc(var(--s)*3) 0;border-bottom:1px solid var(--line)}
.site-header .name{font-style:italic;font-size:20px}
.site-header nav{display:flex;flex-wrap:wrap;gap:calc(var(--s)*2);font-family:var(--sans);font-size:13px;color:var(--muted)}
.site-header nav a:hover,.site-header nav a:focus-visible{color:var(--ink)}

/* ---- intro ---- */
.intro{padding:calc(var(--s)*10) 0 calc(var(--s)*8)}
.intro h1{font-weight:400;font-size:clamp(44px,8vw,72px);line-height:1;letter-spacing:-.015em;margin-bottom:calc(var(--s)*3)}
.intro p{max-width:34em;margin-bottom:calc(var(--s)*3);color:#33362f}
.intro .actions{display:flex;flex-wrap:wrap;align-items:center;gap:calc(var(--s)*3);font-family:var(--sans);font-size:13px}

/* ---- section: rule, label, content ---- */
.section{padding:calc(var(--s)*3) 0 calc(var(--s)*6);border-top:1px solid var(--line)}
.section>.label{display:block;margin-bottom:calc(var(--s)*2)}

/* ---- row: logo | body | date (work entries) ---- */
.row{display:flex;gap:calc(var(--s)*2);align-items:flex-start;padding:calc(var(--s)*2) 0;border-bottom:1px solid var(--line)}
.row:last-child{border-bottom:0}
.row .logo{width:40px;height:40px;flex:0 0 auto;object-fit:contain}
.row .body{flex:1;min-width:0}
.row .title{font-size:20px;font-weight:500}
.row .title .sub{font-weight:400;color:var(--muted)}
.row .role{color:var(--muted)}
.row .desc{margin-top:calc(var(--s)*.5);font-size:16px;color:#33362f}
.row .date{white-space:nowrap;padding-top:4px}

/* ---- grid + card (projects, education) ---- */
.grid{display:grid;grid-template-columns:1fr 1fr;gap:calc(var(--s)*2)}
.card{--tint:var(--tint-1);display:flex;flex-direction:column;justify-content:space-between;gap:calc(var(--s)*2);padding:calc(var(--s)*2.5);border-radius:var(--radius);background:var(--tint);transition:transform .15s}
.card:hover{transform:translateY(-2px)}
.card-outline{background:transparent;border:1px solid var(--line)}
.card .title{font-size:20px;font-weight:500}
.card .desc{margin-top:calc(var(--s)*.5);font-size:16px;color:#33362f}
.card .foot{display:flex;justify-content:space-between;align-items:baseline;gap:var(--s)}
.card .logo{width:40px;height:40px;object-fit:contain;margin-bottom:var(--s)}
.show-all{margin-top:calc(var(--s)*3)}
[hidden]{display:none!important}

/* ---- contact band ---- */
.contact{background:var(--ink);color:var(--paper);padding:calc(var(--s)*8) 0}
.contact h2{font-weight:400;font-size:32px;margin-bottom:var(--s)}
.contact .links{display:flex;flex-wrap:wrap;gap:calc(var(--s)*3);font-family:var(--sans);font-size:13px}
.contact .links a{opacity:.75;text-decoration:underline;text-decoration-color:transparent;text-underline-offset:3px;transition:opacity .15s,text-decoration-color .15s}
.contact .links a:hover,.contact .links a:focus-visible{opacity:1;text-decoration-color:currentColor}

/* ---- one breakpoint ---- */
@media (max-width:640px){
  .grid{grid-template-columns:1fr}
  .row{flex-wrap:wrap}
  .row .date{width:100%;padding:0 0 0 56px}
  .intro{padding:calc(var(--s)*6) 0 calc(var(--s)*5)}
}
</style>
</head>
<body>

<div class="wrap">
  <header class="site-header">
    <a class="name" href="#top">Sambhav Shrestha</a>
    <nav aria-label="Sections">
      <a href="#work">Work</a>
      <a href="#projects">Projects</a>
      <a href="#education">Education</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <section class="intro" id="top">
    <h1>Sambhav<br>Shrestha</h1>
    <p>Software engineer working on the infrastructure behind machine learning. Recently at Meta; now at Stony Brook for a master's in computer science.</p>
    <div class="actions">
      <a class="pill" href="mailto:sambhavshrestha111@gmail.com">Get in touch</a>
      <a class="link" href="./resume.pdf" target="_blank" rel="noopener">Résumé ↗</a>
    </div>
  </section>

  <section class="section" id="work">
    <span class="label">Work</span>
  </section>

  <section class="section" id="projects">
    <span class="label">Projects</span>
  </section>

  <section class="section" id="education">
    <span class="label">Education</span>
  </section>
</div>

<footer class="contact" id="contact">
  <div class="wrap">
    <h2>Let's talk.</h2>
    <div class="links">
      <a href="mailto:sambhavshrestha111@gmail.com">sambhavshrestha111@gmail.com</a>
      <a href="https://linkedin.com/in/sambhav101" target="_blank" rel="noopener">LinkedIn</a>
      <a href="https://github.com/Sambhav101" target="_blank" rel="noopener">GitHub</a>
    </div>
  </div>
</footer>

</body>
</html>
```

- [ ] **Step 2: Run the suite**

Run: `npm test`
Expected: PASS for `page loads`, `has the header and all five section ids`, `no horizontal scroll`, `outbound links`. FAIL for `work has four rows`, `show all reveals`, `every logo image` (no images yet, so `sources.length` is 0).

- [ ] **Step 3: Open it and look**

Run: `open index.html`
Check: ivory page, italic name top-left, four muted nav links top-right, large serif name, one paragraph, a pill and a green link, three empty labelled sections, a dark contact band. Nothing moves.

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "Rewrite index.html skeleton: tokens, header, intro, contact"
```

---

### Task 4: Downscale logos and fill the Work section

**Files:**
- Modify: `images/logo_meta.png`, `images/logo_tarifica.png`, `images/logo_amazon.png`, `images/logo_microsoft.png`, `images/logo_sbu.png`, `images/logo_sjc.png`
- Modify: `index.html` (the `#work` section)

**Interfaces:**
- Consumes: `.row` CSS from Task 3.
- Produces: six logos at 160px long edge, used here and in Task 6.

- [ ] **Step 1: Downscale all six logos in place**

The originals stay in git history (commit `a5b2939`). This overwrite was approved in DESIGN.md.

```bash
cd /Users/sambhav/projects/portfolio_v2
for f in images/logo_*.png; do sips -Z 160 "$f" >/dev/null; done
for f in images/logo_*.png; do sips -g pixelWidth -g pixelHeight "$f" | tail -2 | tr '\n' ' '; echo " $f"; done
```

Expected: every file reports a long edge of 160 or less.

- [ ] **Step 2: Fill the Work section**

In `index.html`, replace

```html
  <section class="section" id="work">
    <span class="label">Work</span>
  </section>
```

with

```html
  <section class="section" id="work">
    <span class="label">Work</span>

    <article class="row">
      <img class="logo" src="images/logo_meta.png" alt="Meta">
      <div class="body">
        <h3 class="title">Meta <span class="sub">via HCL Technologies</span></h3>
        <p class="role">ML Infrastructure Engineer</p>
        <p class="desc">Built Sherlock, an AI assistant that resolves ML-infrastructure questions on its own, plus the monitoring and tooling that keep model training and deployment running.</p>
      </div>
      <span class="meta date">Mar 2024 – Jul 2025</span>
    </article>

    <article class="row">
      <img class="logo" src="images/logo_tarifica.png" alt="Tarifica">
      <div class="body">
        <h3 class="title">Tarifica</h3>
        <p class="role">Software &amp; Data Engineer</p>
        <p class="desc">Built the pipelines that validate and route telecom pricing data into analytics, and refactored a legacy codebase with logging, observability, and tests.</p>
      </div>
      <span class="meta date">Jun 2023 – Feb 2024</span>
    </article>

    <article class="row">
      <img class="logo" src="images/logo_amazon.png" alt="Amazon">
      <div class="body">
        <h3 class="title">Amazon</h3>
        <p class="role">Software Development Engineer</p>
        <p class="desc">Operated production microservices and led a high-traffic service's migration to AWS.</p>
      </div>
      <span class="meta date">Jul 2022 – Mar 2023</span>
    </article>

    <article class="row">
      <img class="logo" src="images/logo_microsoft.png" alt="Microsoft Research">
      <div class="body">
        <h3 class="title">Microsoft Research <span class="sub">DS3</span></h3>
        <p class="role">Data Science Fellow</p>
        <p class="desc">Extended the Financial Times police complaints study with statistical models in R.</p>
      </div>
      <span class="meta date">Jun – Jul 2021</span>
    </article>
  </section>
```

- [ ] **Step 3: Run the suite**

Run: `npm test`
Expected: `work has four rows and education has two cards` still FAILS on the education half; `every logo image` now PASSES (four images, all resolve, all 160px or smaller). `show all reveals` still FAILS.

- [ ] **Step 4: Look at it**

Run: `open index.html`
Check: four rows separated by hairlines, logos legible on ivory (if any logo has a dark box or fringe, note it in the commit message and continue; fixing that is Task 7). Resize the window under 640px: date wraps under the text, aligned with the text column.

- [ ] **Step 5: Commit**

```bash
git add images/ index.html
git commit -m "Downscale logos and add Work section"
```

---

### Task 5: Projects grid with Show-all

**Files:**
- Modify: `index.html` (the `#projects` section and a script before `</body>`)

**Interfaces:**
- Consumes: `.grid`, `.card`, `.show-all`, `.pill`, `.link`, `.label` CSS from Task 3.
- Produces: `button#show-all`; four cards with the `hidden` attribute.

- [ ] **Step 1: Fill the Projects section**

In `index.html`, replace

```html
  <section class="section" id="projects">
    <span class="label">Projects</span>
  </section>
```

with

```html
  <section class="section" id="projects">
    <span class="label">Projects</span>

    <div class="grid">
      <article class="card" style="--tint:var(--tint-1)">
        <div>
          <h3 class="title">Sherlock</h3>
          <p class="desc">An AI assistant built at Meta that answers engineers' ML-infrastructure questions, trained on internal forum data.</p>
        </div>
        <div class="foot">
          <span class="label">LLaMA · RAG · PyTorch</span>
          <span class="meta">internal to Meta</span>
        </div>
      </article>

      <article class="card" style="--tint:var(--tint-2)">
        <div>
          <h3 class="title">Argument Quality Ranking</h3>
          <p class="desc">A model that ranks how well-made a written argument is.</p>
        </div>
        <div class="foot">
          <span class="label">Python · NLP</span>
          <a class="link meta" href="https://github.com/Sambhav101/Argument-Quality-Ranking" target="_blank" rel="noopener">GitHub ↗</a>
        </div>
      </article>

      <article class="card" style="--tint:var(--tint-3)">
        <div>
          <h3 class="title">Raft / Paxos Consensus Simulator</h3>
          <p class="desc">Watch distributed systems agree, disagree, and recover from failures.</p>
        </div>
        <div class="foot">
          <span class="label">Go</span>
          <a class="link meta" href="https://github.com/Sambhav101" target="_blank" rel="noopener">GitHub ↗</a>
        </div>
      </article>

      <article class="card" style="--tint:var(--tint-4)">
        <div>
          <h3 class="title">Pipelined MIPS Processor</h3>
          <p class="desc">A 32-bit five-stage CPU with caches, built from scratch.</p>
        </div>
        <div class="foot">
          <span class="label">Verilog</span>
          <a class="link meta" href="https://github.com/Sambhav101/Cache-enabled-Pipelined-MIPS-Processor" target="_blank" rel="noopener">GitHub ↗</a>
        </div>
      </article>

      <article class="card" style="--tint:var(--tint-1)" hidden>
        <div>
          <h3 class="title">Frequency Prior for Image Generation</h3>
          <p class="desc">Replacing attention with a frequency-based prior in image generation models.</p>
        </div>
        <div class="foot">
          <span class="label">Python · PyTorch</span>
          <a class="link meta" href="https://github.com/Sambhav101/CSE_590_03_VLM" target="_blank" rel="noopener">GitHub ↗</a>
        </div>
      </article>

      <article class="card" style="--tint:var(--tint-2)" hidden>
        <div>
          <h3 class="title">Stock Price Prediction</h3>
          <p class="desc">An LSTM that forecasts stock prices from historical data.</p>
        </div>
        <div class="foot">
          <span class="label">Python</span>
          <a class="link meta" href="https://github.com/Sambhav101/Stocks-Prediction-Using-LSTM" target="_blank" rel="noopener">GitHub ↗</a>
        </div>
      </article>

      <article class="card" style="--tint:var(--tint-3)" hidden>
        <div>
          <h3 class="title">Police Complaints Analysis</h3>
          <p class="desc">Race and gender patterns in police complaints across NYC, Chicago, and Philadelphia, at Microsoft Research.</p>
        </div>
        <div class="foot">
          <span class="label">R</span>
          <a class="link meta" href="https://github.com/msr-ds3/officer-complaints-2021-group-6" target="_blank" rel="noopener">GitHub ↗</a>
        </div>
      </article>

      <article class="card" style="--tint:var(--tint-4)" hidden>
        <div>
          <h3 class="title">Credit Card Fraud Detection</h3>
          <p class="desc">Catching rare fraudulent transactions in an imbalanced dataset with XGBoost and SMOTE.</p>
        </div>
        <div class="foot">
          <span class="label">R</span>
          <a class="link meta" href="https://github.com/Sambhav101/Credit-Card-Fraud-Detection" target="_blank" rel="noopener">GitHub ↗</a>
        </div>
      </article>
    </div>

    <div class="show-all">
      <button class="pill" id="show-all" type="button">Show all 8 projects</button>
    </div>
  </section>
```

- [ ] **Step 2: Add the one script**

In `index.html`, insert immediately before `</body>`:

```html
<script>
  // The only script on the page: reveal the four collapsed project cards.
  document.getElementById('show-all').addEventListener('click', function () {
    document.querySelectorAll('#projects .card[hidden]').forEach(function (card) { card.hidden = false; });
    this.hidden = true;
  });
</script>
```

- [ ] **Step 3: Run the suite**

Run: `npm test`
Expected: `show all reveals eight project cards and hides the button` PASSES. Only `work has four rows and education has two cards` still FAILS (education half).

- [ ] **Step 4: Look at it**

Run: `open index.html`
Check: two columns of tinted cards, four visible, button below. Click the button: four more appear in the same grid, button disappears. Hover a card: it lifts 2px, nothing else moves.

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "Add Projects grid with Show-all"
```

---

### Task 6: Education cards

**Files:**
- Modify: `index.html` (the `#education` section)

**Interfaces:**
- Consumes: `.grid`, `.card`, `.card-outline`, `.meta`, `.desc` CSS from Task 3; `logo_sbu.png` and `logo_sjc.png` from Task 4.

- [ ] **Step 1: Fill the Education section**

In `index.html`, replace

```html
  <section class="section" id="education">
    <span class="label">Education</span>
  </section>
```

with

```html
  <section class="section" id="education">
    <span class="label">Education</span>

    <div class="grid">
      <article class="card card-outline">
        <div>
          <img class="logo" src="images/logo_sbu.png" alt="Stony Brook University">
          <h3 class="title">Stony Brook University</h3>
          <p class="desc">M.S. Computer Science</p>
        </div>
        <span class="meta">2025 – 2027</span>
      </article>

      <article class="card card-outline">
        <div>
          <img class="logo" src="images/logo_sjc.png" alt="St. Joseph's College New York">
          <h3 class="title">St. Joseph's College New York</h3>
          <p class="desc">B.S. Computer Science &amp; Mathematics</p>
          <p class="desc">GPA 3.93 · President's Scholar</p>
        </div>
        <span class="meta">2018 – 2022</span>
      </article>
    </div>
  </section>
```

- [ ] **Step 2: Run the full suite**

Run: `npm test`
Expected: `7 passed`. This is the first fully green run.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "Add Education cards; smoke suite green"
```

---

### Task 7: Visual pass on a real viewport and logo fix-ups

**Files:**
- Possibly modify: `index.html` (CSS only, within the closed token list), `images/logo_*.png`

**Interfaces:**
- Consumes: everything above. Produces nothing new; this is the "does it look like the mockup" gate.

- [ ] **Step 1: Screenshot desktop and phone with Playwright**

Create a throwaway script in the scratchpad (not in the repo):

```js
// /private/tmp/claude-501/-Users-sambhav-projects-portfolio-v2/eac5f5fa-91b8-4e41-9f18-e5d74bb4e188/scratchpad/shots.js
const { chromium } = require('@playwright/test');
const path = require('path');
(async () => {
  const url = 'file://' + path.resolve('/Users/sambhav/projects/portfolio_v2/index.html');
  const browser = await chromium.launch();
  for (const [name, w, h] of [['desktop', 1280, 900], ['phone', 390, 844]]) {
    const page = await browser.newPage({ viewport: { width: w, height: h } });
    await page.goto(url);
    await page.waitForTimeout(800); // let Google Fonts arrive
    await page.screenshot({ path: __dirname + '/' + name + '.png', fullPage: true });
  }
  await browser.close();
})();
```

Run: `cd /Users/sambhav/projects/portfolio_v2 && node /private/tmp/claude-501/-Users-sambhav-projects-portfolio-v2/eac5f5fa-91b8-4e41-9f18-e5d74bb4e188/scratchpad/shots.js`
Then open both PNGs with the Read tool and compare against the "3 · Classic serif" mockup in `.superpowers/brainstorm/49588-1789069233/content/type-color.html`.

- [ ] **Step 2: Fix only what the screenshots show**

Allowed fixes: spacing values, `clamp()` bounds, wrapping at 390px, logo padding. For a logo that reads badly on ivory (dark box, jagged edge), re-export it on a transparent background at 160px, or wrap it in a white tile by adding `background:#fff;border-radius:6px;padding:4px` to that one `<img>` via a shared `.logo-tile` class added to the stylesheet (one class, applied to any logo that needs it; never a per-logo rule).

Not allowed: new colors, new fonts, any animation, any new section.

- [ ] **Step 3: Run the suite again**

Run: `npm test`
Expected: `7 passed`.

- [ ] **Step 4: Commit**

```bash
git add index.html images/
git commit -m "Visual pass: spacing and logo fix-ups from screenshots"
```

(Skip the commit if nothing changed.)

---

### Task 8: Update project notes and open the PR

**Files:**
- Rewrite: `CLAUDE.md` (local, gitignored) and `AGENTS.md` (local, untracked) with the same content
- Modify: `DESIGN.md` (status line only)

**Interfaces:**
- Consumes: the finished site.

- [ ] **Step 1: Rewrite CLAUDE.md**

Overwrite `CLAUDE.md` with:

```markdown
# Portfolio v3 — CLAUDE.md

## Project
Single-file portfolio: `index.html`. No build step. Only external dependency is Google Fonts (Newsreader, Inter).
Open with: `open index.html`. Test with: `npm test` (Playwright smoke suite in `tests/smoke.spec.js`).
Design spec: `DESIGN.md`. Hosted on Vercel at sambhavshrestha.com; `api/` and `vercel.json` belong to a retired chatbot and are left in place on purpose.

## File structure
```
portfolio_v2/
├── index.html                 # the whole site
├── DESIGN.md                  # approved design spec (living doc)
├── tests/smoke.spec.js        # Playwright smoke test
├── playwright.config.js
├── .github/workflows/test.yml # runs npm test on push/PR
├── images/logo_*.png          # six logos, 160px long edge
├── resume.tex / resume.pdf
├── api/, vercel.json          # retired chatbot backend, untouched
└── CNAME                      # legacy GitHub Pages record
```

## Design tokens (closed list; do not add colors or fonts)
```
--paper:#fbf9f4 --ink:#1a1c19 --muted:#6a6e67 --line:#e6e3da --accent:#2f6b45
--tint-1:#e6ede4 --tint-2:#f0e8d9 --tint-3:#e3e8ee --tint-4:#eee3e8
--serif: Newsreader   --sans: Inter   --measure:880px  --radius:10px  --s:8px
```
Light theme only. No dark mode, no toggle.

## Page structure
Header (`.site-header`, static) → Intro (`#top`) → Work (`#work`) → Projects (`#projects`) → Education (`#education`) → Contact (`#contact`, dark band).

## Reusable patterns (add entries by copying a block, never by adding CSS)
- `.section` — rule, uppercase `.label`, content.
- `.row` — logo | body (`.title`, `.role`, `.desc`) | `.date`. All Work entries.
- `.card` — tinted via `style="--tint:var(--tint-N)"`; `.title`, `.desc`, `.foot`. All 8 projects. `.card-outline` for the 2 education cards.
- `.pill`, `.link` — the only interactive text styles.

## Behaviour
- Hover only: links underline in accent, cards lift 2px. No scroll or load animation.
- One script: `#show-all` button removes `hidden` from the 4 collapsed project cards and hides itself.
- One breakpoint at 640px: grid → 1 column, row date wraps under text.

## Content
Work: Meta via HCL (Mar 2024–Jul 2025), Tarifica (Jun 2023–Feb 2024), Amazon (Jul 2022–Mar 2023), Microsoft Research DS3 (Jun–Jul 2021).
Projects (featured): Sherlock, Argument Quality Ranking, Raft/Paxos Simulator, Pipelined MIPS. Hidden: Frequency Prior, Stock LSTM, Police Complaints, Credit Card Fraud.
Education: SBU M.S. CS 2025–2027; SJC B.S. CS & Math 2018–2022, GPA 3.93, President's Scholar.
Copy one-liners were drafted by Claude from the v2 bullets and are still marked DRAFT in DESIGN.md until the user edits them.

## Notes
- Raft/Paxos GitHub link points to the profile root; update when the repo exists.
- v2 (dark, particles, timelines, VS Code cards, chatbot widget) lives in git history at `a5b2939`.
```

Then copy it: `cp CLAUDE.md AGENTS.md`.

- [ ] **Step 2: Mark the spec as built**

In `DESIGN.md`, change the line `Status: approved 2026-09-10. Living doc; update when a decision changes.` to `Status: approved 2026-09-10, built on branch feat/v3-redesign. Living doc; update when a decision changes.`

- [ ] **Step 3: Commit and push**

```bash
git add DESIGN.md
git commit -m "Mark DESIGN.md as built"
git push -u origin feat/v3-redesign
```

- [ ] **Step 4: Open the PR and confirm CI**

```bash
gh pr create --title "v3 redesign: calm editorial one-page site" --body "Implements DESIGN.md. Replaces the v2 page with layout C / classic serif. Adds Playwright smoke test and CI. Closes the v3 milestone issues."
gh pr checks --watch
```

Expected: the `test / smoke` check goes green. Then share the Vercel preview URL from the PR page with the user for a phone check before merging. Merging is the user's call.

---

## Self-review

**Spec coverage.** Header, intro, work rows, four featured plus Show-all, education cards with GPA and award, contact band: Tasks 3 to 6. Tokens, two fonts, 880px, one breakpoint, hover-only motion: Task 3 CSS. Playwright smoke test with console errors, five ids, Show-all count, 390px width, link and résumé checks: Task 2. GitHub Actions on push: Task 1. Logo downscale to 160px: Task 4, verified by the image test. Backend untouched: no task touches `api/` or `vercel.json`. Draft copy: verbatim from the appendix. Rollout on a branch, preview, merge by user: Tasks 1 and 8.

One spec correction made while planning: DESIGN.md said "all six section ids"; the header has no id, so the test checks the header class plus five ids. The spec test list should be read that way.

**Placeholders.** None. Every code step contains the full content.

**Type consistency.** `button#show-all` in the test (Task 2), the HTML (Task 5), and the script (Task 5). `#projects .card` with `hidden` attribute in Task 5 matches the `:visible` counts in Task 2 and the `[hidden]{display:none!important}` rule in Task 3. `.row` inside `#work` (Task 4) matches the `#work .row` count. `.card` inside `#education` (Task 6) matches `#education .card`. `.site-header` in Task 3 matches Task 2.
